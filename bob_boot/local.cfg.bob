[buildout]
extends = local_{{{ local.which_env }}}.cfg

parts +=
{{% if local.sublime %}}
    codeintel
{{% endif %}}

backupdir = {{{ local.backupdir }}}
vardir = {{{ local.vardir }}}
logdir = ${buildout:vardir}/log
